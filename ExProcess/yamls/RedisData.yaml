apiVersion: apps/v1
kind: Deployment
metadata:
  name: redisdatadp
spec:
  selector:
    matchLabels:
      app: redisdata
  template:
    metadata:
      labels:
        app: redisdata
    spec:
      containers:
      - name: redis
        image: redis:alpine
        ports:
        - containerPort: 6379
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: vm
                operator: In
                values:
                - RA  # 假设VMA是目标节点的标签